#include <iostream>
#include <thread>
#include <vector>
#include <queue>
#include <mutex>


void sum_queue (std::queue<int>& num_queue,
                int& sum,
                std::mutex& mtx) {

};

int main ()
{
    std::queue<int> input_data;
    std::mutex mtx;
    int sum1(0), sum2(0);


    return 0;
}
